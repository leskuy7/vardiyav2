# Local dev (API on port 4000)
NEXT_PUBLIC_API_URL=http://localhost:4000/api

# Üretimde demo giriş butonlarını gizlemek için: NEXT_PUBLIC_DEMO_ENABLED=false

# Production (Vercel): set NEXT_PUBLIC_API_BASE to your Railway API root, e.g.:
# NEXT_PUBLIC_API_BASE=https://vardiyav2-api-production.up.railway.app
# Or set NEXT_PUBLIC_API_URL to the full API URL (e.g. https://xxx.railway.app/api).

# LLM/AI anahtarları ve model adı web tarafına konulmamalı (server-only).

CSP_REPORT_TOKEN=replace-with-same-token-as-api
